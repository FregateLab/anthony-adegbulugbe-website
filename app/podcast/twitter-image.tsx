// Reuse the Open Graph card for Twitter/X so the same branded preview is
// served. Next auto-sets twitter:card to summary_large_image at 1200x630.
export { default, alt, size, contentType, runtime } from "./opengraph-image"
